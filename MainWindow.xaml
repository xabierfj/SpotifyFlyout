<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SpotifyFlyout.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpotifyFlyout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SpotifyFlyout">


    <Grid x:Name="Test" DataContext="{x:Bind ViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Album Art -->
        <!-- <Image Grid.Row="0" Width="64" Height="64" Margin="10" Source="{Binding MediaData.AlbumArtUrl}" />  --> 

        <!-- Track Info -->
        <StackPanel Grid.Row="1" Margin="10" DataContext="{x:Bind ViewModel}">
            <TextBlock Text="{Binding MediaData.Title}" FontSize="16" FontWeight="Bold" />
            <TextBlock Text="{Binding MediaData.Artist}" FontSize="14" Foreground="Gray" />
        </StackPanel>
    </Grid>
    
</Window>
